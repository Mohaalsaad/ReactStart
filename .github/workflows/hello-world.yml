name: Hello World
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo "Hello, GitHub Actions!"
      - name: Intentional Error
        run: non-existent-command  # Introduce a deliberate error   
